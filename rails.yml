require:
  - rubocop-rails

inherit_gem:
  standard-rails: config/base.yml

# standard-rails overrides
Rails/DynamicFindBy:
  Enabled: false

Rails/EnvironmentVariableAccess:
  Enabled: false

Rails/PluckId:
  Enabled: false
